import { bubbleEventsAlias, basePropAlias } from '../prop';
import { PickerViewColumnAlias as Alias } from '../type';

const alias: Alias = {
  ...basePropAlias,
  ...bubbleEventsAlias,
};

export default alias;
