{
  "name": "@bytedance/mona-client-web",
  "version": "0.0.1",
  "description": "web for mona",
  "main": "./dist/index.js",
  "scripts": {
    "build": "yarn clean && yarn copy && yarn api-copy && tsc -b",
    "start": "tsc -b -w",
    "clean": "rm -rf dist",
    "copy": "copyfiles -u 2 ./src/components/**/*.less ./dist/components",
    "api-copy": "copyfiles -u 3 ./src/apis/components/**/*.css ./dist/apis/components"
  },
  "keywords": [
    "web",
    "mona"
  ],
  "dependencies": {
    "@bytedance/mona": "^0.1.3",
    "classnames": "^2.3.1",
    "copyfiles": "^2.4.1"
  },
  "author": "zhangxiaowei.chris@bytedance.com",
  "license": "MIT"
}
