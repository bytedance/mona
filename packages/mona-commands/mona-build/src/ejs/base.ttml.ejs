<template name="mona-base">
  <template tt:for="{{root.children}}" tt:key="*this" is="{{'mona-' + root.nodes[item].t}}" data="{{node: root.nodes[item]}}" />
</template>
<%for (let name of ejsParamsMap.keys()) {%>
<template name="mona-<%=name%>">
  <<%=name%> 
    <%_for(let i in ejsParamsMap.get(name).alias){-%>
    <%_if(ejsParamsMap.get(name).defaultProps[i] !== undefined){-%>
    <%=ejsParamsMap.get(name).alias[i]%>="{{node.p['<%=i%>'] === undefined ? <%-ejsParamsMap.get(name).defaultProps[i]%> : node.p['<%=i%>']}}" 
    <%_} else{-%>
    <%=ejsParamsMap.get(name).alias[i]%>="{{node.p['<%=i%>']}}" 
    <%_}}-%>
  >
  <% if(!noChildElements.has(name)) { -%>
    <template 
      is="{{ 'mona-' + node.n[item].t }}" 
      tt:for="{{ node.c }}"
      tt:key="{{ item }}"
      data="{{ node: node.n[item] }}" 
    />
  <%_}-%></<%=name%>>
</template><%};-%>
<template name="mona-ptext">
  <block>{{ node.te }}</block>
</template>


