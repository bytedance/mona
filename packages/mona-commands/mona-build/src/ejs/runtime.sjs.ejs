var pageNodesMap = {}


function execTask(task, pageId) {
    var type = task.method;
    if (type === 'appendChildToContainer') {
        pageNodesMap[pageId] = task.children;
    }
}
function processTask(task){
    return task.node
}

function parseNodes(tasks, pageId) {
   
    tasks.forEach(task=>{
        if(pageNodesMap[pageId]){
        } else {
          pageNodesMap[pageId] = processTask(task)
        }
    })
    return pageNodesMap[pageId];
}

module.exports = {
    parseNodes: parseNodes
}