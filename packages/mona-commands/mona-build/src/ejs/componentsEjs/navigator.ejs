<template name="mona-ttnavigator">
  <navigator 
    <%_ for (let i in props) { -%>
    <%_ if(defaultProps[i] !== undefined)  {  -%>
    <%=props[i]%>="{{node.props['<%=i%>'] === undefined ? <%-defaultProps[i]%> : node.props['<%=i%>']}}" 
    <%_   } else {  -%>
    <%=props[i]%>="{{node.props['<%=i%>']}}" 
    <%_ }
    } -%>
  >
    <block  
      tt:for="{{ node.children }}"
      tt:key="{{ item.key }}"
    >
      <template is="{{ 'mona-' + node.nodes[item].type }}"   data="{{ node: node.nodes[item] }}" />
    </block>
  </navigator>
</template>

<!--字节小程序的bug mona-navigator 不会渲染  -->
<template name="mona-navigator">
  <navigator 
    <%_ for (let i in props) { -%>
    <%_ if(defaultProps[i] !== undefined)  {  -%>
    <%=props[i]%>="{{node.props['<%=i%>'] === undefined ? <%-defaultProps[i]%> : node.props['<%=i%>']}}" 
    <%_   } else {  -%>
    <%=props[i]%>="{{node.props['<%=i%>']}}" 
    <%_ }
    } -%>
  >
    <block  
      tt:for="{{ node.children }}"
      tt:key="{{ item.key }}"
    >
      <template is="{{ 'mona-' + node.nodes[item].type }}"   data="{{ node: node.nodes[item] }}" />
    </block>
  </navigator>
</template>